# Replicate API (video generation)
REPLICATE_API_TOKEN=
REPLICATE_MODEL_VERSION=

# Anthropic API (dialogue fallback — optional, falls back to static responses)
ANTHROPIC_API_KEY=

# Neon PostgreSQL (character storage — auto-injected by Vercel/Neon integration)
DATABASE_URL=

# Toss auth proxy
TOSS_AUTH_URL=https://auth.oneclack.com
